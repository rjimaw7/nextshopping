import LandingPage from '@/components/Home/LandingPage';

const page = () => {
  return (
    <section className="container min-h-screen">
      <LandingPage />
    </section>
  );
};

export default page;
